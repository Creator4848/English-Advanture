# Next.js Frontend – Vercel Production Config
# API calls go to the same domain (relative URLs) — no separate backend domain needed.
# Vercel rewrites /api/* → api/index.py automatically.

NEXT_PUBLIC_API_URL=/api
NEXT_PUBLIC_WS_URL=
